# 📚 Структуры данных на Java

**Обучающий проект для реализации структур данных с нуля**  

Этот проект — ваша лаборатория для изучения структур данных на Java без стандартных библиотек (`java.util.*`). Цель — разобраться, как работают списки, стеки, очереди и деревья "под капотом", освоить generics и понять временную сложность операций. Идеально для студентов, подготовки к собеседованиям и всех, кто хочет углубить знания! 🚀

## 🎯 Цели проекта
- Реализовать ключевые структуры данных с нуля.
- Создать собственную библиотеку для использования в других проектах.
- Практиковать generics, инкапсуляцию, наследование и композицию, абстракцию .
- Анализировать временную сложность операций (Big-O).
- Избежать типичных ошибок (`NullPointerException`, утечки памяти).
- Освоить Junit5

## 🧱 Реализованные структуры

| Структура            | Статус         | Основные методы                                                                    |
|----------------------|----------------|------------------------------------------------------------------------------------|
| **SinglyLinkedList** | ✅ Завершено    | `add`,`removeAt`, `size`, `show`, `getValueAt` , `isEmpty`, `toArray`              |
| **DoublyLinkedList** | ✅ Завершено    | `add`,`removeAt`, `size`, `show`, `getValueAt` , `isEmpty`, `toArray`              |
| **Stack**            | ✅ Завершено    | `push`, `pop`, `peek`, `isEmpty`,                                                  |
| **BinaryTree**       | ✅ Завершено    | `insert`, `search`, `delete`, `update`, `show (inoreder)`, `BFS`, `DFS`            |
| **LinkedQueue**      | ✅ Завершено    | `enqueue`, `dequeue`, `peek`, `isEmpty`                                            |
| **Deque**            | ✅ Завершено    | `addFirst`, `addLast`, `removeFirst`, `removeLast`,`peekFirst`, `peekLast`, `size` |
| **AVLTree**          | ✅ Завершено    | `insert`,`delete`, `update`, `show (inoreder)`, `BFS`, `DFS`                       |
| **ReadBlackTree**    | ✅ Завершено    | `insert`, `search`, `delete`, `update`, `show (inoreder)`, `BFS`, `DFS`            |
| **MaxHeap**          | ✅ Завершено    | `add` , `clear`, `size`, `toArray`, `getMax`, `extractMax`                         |
| **MinHeap**          | ✅ Завершено    | `add` , `clear`, `size`, `toArray`, `getMin`, `extractMin`                         |
| **HashMap**          | ✅ Завершено    | `add`, `remove`, `contains` , `show` `get`                                         |
| **PriorityQueue**    | 🔍 Планируется | 🔍 Планируется                                                                     |
| **HashSet**          | 🔍 Планируется | 🔍 Планируется                                                                     |

## 📋 Как начать
1. Склонируйте репозиторий:
   ```bash
   git clone https://github.com/GuliGuli228/data_structures.git
   ```
2. Убедитесь, что установлена Java (JDK 11+).
3. Откройте проект в IDE (IntelliJ IDEA, Eclipse) и запустите примеры из папки `src`.
## Или используйте Maven или Gradle
### Для Maven
1. Добавьте в pom.xml
```xml
	<repositories>
		<repository>
		    <id>jitpack.io</id>
		    <url>https://jitpack.io</url>
		</repository>
	</repositories>
```
2. Добавьте в зависимости
```xml
	<dependency>
	    <groupId>com.github.GuliGuli228</groupId>
	    <artifactId>data_structures</artifactId>
	    <version>1.1.1</version>
	</dependency>
```

### Для Gradle
1. Добавьте в корневую папку `settings.gradle` в конец репозиториев
```groovy
	dependencyResolutionManagement {
		repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
		repositories {
			mavenCentral()
			maven { url 'https://jitpack.io' }
		}
	}
```
2. Добавьте зависимость
```groovy
	dependencies {
	        implementation 'com.github.GuliGuli228:data_structures:Tag'
	}
```
## Описание структур данных 
### ✅ Списки
- **SinglyLinkedList**: Поддерживает вставку, удаление, поиск, вставку по индексу и вывод.
- **DoublyLinkedList**: Те же операции, но с двунаправленными связями для большей гибкости.
- **CircularList**: Те же операции, но последний элемент указывает на первый
- Поддерживает:
  - `add` — добавление элемента.
  - `removeAt` — удаление элемента по индексу.
  - `size` — проверка размера списка.
  - `getValueAt` — получение элемента по индексу.
  - `isEmpty` — проверка на пустоту.
  - `toArray` — получение списка в виде массива.


### ✅ Стек
- Реализация на основе связанного списка.
- Поддерживает: 
  - `push` — добавление элемента.
  - `pop` — удаление верхнего элемента.
  - `peek` — просмотр верхнего элемента.
  - `isEmpty` — проверка на пустоту.

### ✅ Очередь
- **LinkedQueue**: Очередь на основе связного списка
- Поддерживает:
  - `enqueue` - добавление элемента в конец очереди
  - `dequeue` - удаление элемента из очереди
  - `peek` - просмотр верхнего элемента
  - `isEmpty` - проверка на пустоту

- **Deque**: Двусторонняя очередь на основе связного списка
- Поддерживает:
  - `addFirst` - добавление элемента в начало очереди
  - `addLast` - добавление элемента в конец очереди
  - `removeFirst` - удаление первого элемента очереди
  - `removeLast` - удаление последнего элемента очереди
  - `peekFirst` - просмотр верхнего элемента
  - `peekFirst` - просмотр нижнего элемента
  - `isEmpty` - проверка на пустоту

### ✅ Дерево
- **BinaryTree**: Двоичное дерево поиска. Поддерживает вставку, удаление, поиск в ширину, поиск в глубину, обновление, прямой обход. Запрещает дубликаты.
- **AVLTree**: Те же операции, но с поддержкой  автобалансировки при помощи правого и левого поворота. Запрещает дубликаты.
- **RedBlackTree**: Те же операции, но с поддержкой автобалансировки при помощи правого и левого поворота и перекрашивания узлов. Запрещает дубликаты
- Поддерживает: 
  - `add` - добавить элемент в дерево 
  - `deleteByValue`- удалить элемент по значению
  - `update` - обновить значение элемента
  - `show` - прямой обход
  - `BFS` - поиск в ширину
  - `DPS` - поиск в глубину
  - `isEmpty` - проверка на пустоту

### ✅ Куча
- **MaxHeap**: Куча с максимальным элементом сверху на основе массива, поддерживает автоизменение размера массива в зависимости от заполненности
- **MinHeap**: Куча с минимальным элементом сверху на основе массива, поддерживает автоизменение размера массива в зависимости от заполненности
- Поддерживает:
  - `add` - добавление элемента в кучу
  - `clear` - полная очистка кучи 
  - `size` - получение размера кучи
  - `toArray` - получение кучи в виде массива
  - `getMax/getMin` - просмотр максимального/минимального элемента кучи
  - `extractMax/extractMin` - получение максимального/минимального элемента в куче

### ✅ Ассоциативный массив 
- **HashMap**: Ассоциативный массив. Хэш функция - xxHash. Способ разрешения коллизий - chaining на основе RedBlackTree
- Поддерживает:
  - ``add`, ``, `contains` , `show` `get`
  - `add` - добавление элемента в таблицу
  - `remove` - удаление элемента из таблицы
  - `contains` - проверка наличия элемента в таблице (по ключу)
  - `show` - вывод таблицы
  - `getMax/getMin` - просмотр максимального/минимального элемента кучи
  - `get` - получение значения элемента по ключу

## 🧠 Для кого этот проект
- Студенты, изучающие алгоритмы и структуры данных.
- Разработчики, готовящиеся к техническим собеседованиям.
- Энтузиасты, желающие понять работу коллекций.

## 🚀 Планы на будущее
- Перейти с абстрактных методов на интерфейсы (где это уместно)
- Расширить список структур данных
- Переписать рекурсивные методы на итеративные

## Архитектура проекта
**Семейство списков**
<img width="827" height="1169" alt="Image" src="https://github.com/user-attachments/assets/a8c4bf5a-7959-4294-a5be-c934f811a6a3" />
**Семейство деревьев**
<img width="827" height="1169" alt="Image" src="https://github.com/user-attachments/assets/5bd77ddf-8167-48c0-9f3c-5b8c17a8389e" />

## 🤝 Как внести вклад
Хотите помочь? Форкните репозиторий, добавьте свои улучшения и отправьте Pull Request! 😊

## 👤 Автор
**GuliGuli228**  
📧 Связь: [GitHub](https://github.com/GuliGuli228)

## 📜 Лицензия
© 2025 GuliGuli228. Все права защищены.